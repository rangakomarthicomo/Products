{% extends '::base.html.twig' %}

{% block body -%}

<div class="container">
    <div class="content">
      <div class="row">
        <div class="login-form">
    <h1>Brand list</h1>

    <table class="table-striped" cellspacing="15" cellpadding="15">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Country</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('brand_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.country }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('brand_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('brand_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('brand_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    </div>
              </div>
            </div>
        </div>
    {% endblock %}
